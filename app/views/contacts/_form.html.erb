<%= form_with(model: contact, local: true) do |form| %>
  <% if contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-signin">
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :contact_name ,class: "reauth-email" %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :contact_email ,class: "reauth-email" %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :contact_content ,class: "reauth-email" %>
  </div>
  
  <div class="actions">
    <%= form.submit "Send" ,class:"btn btn-lg btn-primary btn-block btn-signin"%>
  </div>
  
<% end %>
</div>
